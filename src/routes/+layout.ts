/** Necessario per `@sveltejs/adapter-static`: pagine generate in build. */
export const prerender = true;

/** GitHub Pages serve `/path` come `/path/index.html`. */
export const trailingSlash = 'always';
